import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cuidapet_fornecedor/app/modules/schedules/schedules_controller.dart';
import 'package:cuidapet_fornecedor/app/modules/schedules/schedules_module.dart';

void main() {
  initModule(SchedulesModule());
  // SchedulesController schedules;
  //
  setUp(() {
    //     schedules = SchedulesModule.to.get<SchedulesController>();
  });

  group('SchedulesController Test', () {
    //   test("First Test", () {
    //     expect(schedules, isInstanceOf<SchedulesController>());
    //   });

    //   test("Set Value", () {
    //     expect(schedules.value, equals(0));
    //     schedules.increment();
    //     expect(schedules.value, equals(1));
    //   });
  });
}
