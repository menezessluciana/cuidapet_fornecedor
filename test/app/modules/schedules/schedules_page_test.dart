import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_fornecedor/app/modules/schedules/schedules_page.dart';

void main() {
  testWidgets('SchedulesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SchedulesPage(title: 'Schedules')));
    //  final titleFinder = find.text('Schedules');
    //  expect(titleFinder, findsOneWidget);
  });
}
