import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_fornecedor/app/modules/schedule/schedule_page.dart';

void main() {
  testWidgets('SchedulePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SchedulePage(title: 'Schedule')));
    //  final titleFinder = find.text('Schedule');
    //  expect(titleFinder, findsOneWidget);
  });
}
